# the one real runtime dependency, a lightweight pure python package that makes the UI look so nice on Mac
tkmacosx

# for running tests, you'll need the nose and coverage packages
coverage
coveralls  # really just for github, but it's fine
nose

# for building wheels, mostly from github, but it's fine
wheel

# for polishing up the Pip install
PLAN-Tools==0.6
